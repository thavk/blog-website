# Use the official PostgreSQL image from Docker Hub
FROM postgres:15

# Optional: Set environment variables for default DB, user, and password
ENV POSTGRES_DB=myapp
ENV POSTGRES_USER=thafak
ENV POSTGRES_PASSWORD=123321

# Expose default PostgreSQL port
EXPOSE 5432

# The official postgres image runs the server by default, so no CMD needed
